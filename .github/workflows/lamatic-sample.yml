name: CI Pipeline

on:
  push:
    branches: 
      - litellm-main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run tests
        run: |
          ls